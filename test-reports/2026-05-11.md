# 2026-05-11

## 20:40 KST - KMSF sign-in dev server error investigation

- timestamp: 2026-05-11 20:40:34 KST
- summary: Investigated the reported Nuxt-like error on the KMSF sign-in page when running `npm run dev`. The reproduced browser error was a Next.js dev HMR WebSocket failure, not a Nuxt runtime error.
- changed files: `test-reports/2026-05-11.md`
- commands actually run:
  - `npm run dev`
  - `curl -i http://127.0.0.1:3000/sign-in`
  - Playwright one-off browser checks for `http://127.0.0.1:3000/sign-in` and `http://localhost:3000/sign-in`
  - `curl -i http://127.0.0.1:3000/_next/webpack-hmr`
  - `npm run test:e2e -- tests/e2e/auth-validation-i18n.spec.ts --workers=1`
  - Source inspection commands for `apps/kmsf/package.json`, `apps/kmsf/next.config.ts`, `apps/kmsf/playwright.config.ts`, sign-in pages, middleware, proxy, auth, and Supabase helper files
- pass/fail result:
  - PASS: `http://localhost:3000/sign-in` loaded with HTTP 200 and browser console showed `[HMR] connected`.
  - PASS: `http://127.0.0.1:3000/sign-in` loaded with HTTP 200, reproducing the dev-console HMR WebSocket error.
  - PASS: Next dev server log confirmed cross-origin blocking for `/_next/webpack-hmr` from `127.0.0.1`.
  - FAIL: `npm run test:e2e -- tests/e2e/auth-validation-i18n.spec.ts --workers=1` failed because the expected validation text was not visible after the scripted interaction. This is a separate validation/E2E behavior to triage if requested.
- residual risks or blockers:
  - The reported visible error is attributable to using `127.0.0.1` against a dev server initialized as `localhost`. Using `http://localhost:3000/sign-in` avoids the HMR error.
  - If `127.0.0.1` must be supported in development or E2E, `allowedDevOrigins` in Next config or the Playwright base URL/host should be adjusted and verified separately.

## 20:58 KST - KMSF localhost sign-in dev error re-investigation

- timestamp: 2026-05-11 20:58:00 KST
- summary: Re-checked the original sign-in dev error with the assumption that the browser entered through `localhost`. The previous cross-origin diagnosis was incomplete. `allowedDevOrigins` is still required for `127.0.0.1` access, but the reproduced `localhost` terminal failure was a Turbopack dev panic reporting `Next.js package not found`. The dev script was switched to webpack for local development, and `127.0.0.1` was added to Next dev allowed origins.
- changed files:
  - `apps/kmsf/next.config.ts`
  - `apps/kmsf/package.json`
  - `test-reports/2026-05-11.md`
- commands actually run:
  - `npm run lint`
  - `npm run test:run`
  - `npm run build`
  - `npm run dev -- --port 3125`
  - Playwright one-off browser check for `http://localhost:3125/sign-in` and `http://127.0.0.1:3125/sign-in`
  - `curl -I http://localhost:3125/sign-in`
  - Source inspection commands for Next local docs, `apps/kmsf/next.config.ts`, `apps/kmsf/package.json`, Playwright config, and auth/e2e files
- pass/fail result:
  - PASS: `npm run lint`.
  - PASS: `npm run test:run` with 11 files and 39 tests passing.
  - PASS: `npm run build` exited 0. Build emitted existing Turbopack NFT trace warnings around `local-json-auth-store.ts` and `process.cwd()`.
  - PASS: `npm run dev -- --port 3125` started as `next dev --webpack --port 3125`.
  - PASS: Browser verification loaded both `http://localhost:3125/sign-in` and `http://127.0.0.1:3125/sign-in` with HTTP 200, title `KMSF Next Template`, heading `KMSF 로그인`, and only React DevTools info plus `[HMR] connected` console messages.
- residual risks or blockers:
  - The underlying Turbopack dev panic remains unresolved if someone explicitly runs `next dev` without `--webpack`.
  - Webpack dev mode may be slower than Turbopack.
  - `npm run build` passes, but the existing NFT trace warning should be triaged separately if Turbopack production build warnings must be eliminated.
