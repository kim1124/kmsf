# Gridstack Docs Example Composition Tasks

## Status

- ask gate: clear
- Implementation status: not started

## Tasks

1. Add RED coverage for per-widget `movable` and `resizable` mapping.
2. Update public widget types and adapter mapping.
3. Add or update focused Vitest coverage for lock precedence.
4. Split example data and live examples by page purpose.
5. Add package-local Dialog and Select example UI components.
6. Implement Basic example with 12 widgets on 12 columns.
7. Implement CRUD example with three default widgets and Dialog add/update/delete.
8. Implement Layout examples for save/restore, column Select, and global lock/unlock.
9. Implement Widget example for per-widget move and resize locks.
10. Implement Complete smoke example.
11. Expand API page by public API category.
12. Update Playwright specs for the new docs/example routes and controls.
13. Run focused Vitest and Playwright checks.
14. Run `npm --workspace=@kmsf/gridstack run verify`.
15. Run `npm --workspace=@kmsf/gridstack run verify:full`.
16. Update `reports/2026-07-01.md` with verification evidence and residual risks.
