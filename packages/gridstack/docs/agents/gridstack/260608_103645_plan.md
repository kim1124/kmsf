# GridStack Resize Boundary Exit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 위젯 resize 중 포인터가 GridLayout 또는 브라우저 boundary를 벗어나 document `mouseup`이 누락되어도 GridStack interaction이 종료되고 pending layout sync가 flush되도록 한다.

**Architecture:** 먼저 Playwright에서 browser-boundary exit RED 테스트를 추가한다. 이후 `src/gridstack/adapter.ts` 안에서 active drag/resize 동안만 document/window guard listener를 붙이고, boundary exit 신호에서 synthetic `mouseup`으로 GridStack 자체 cleanup path를 우선 실행한다. GridStack stop event가 다음 frame까지 오지 않으면 adapter의 기존 stop/flush 경로로 fallback한다.

**Tech Stack:** React 19, TypeScript, GridStack 12.6.0, Vite, Playwright, Vitest.

---

## User Decision Check

추가 사용자 질문은 필요하지 않다.

- Public API, props, serialized layout schema, example UX text를 변경하지 않는다.
- 동작 범위는 내부 adapter cleanup 보강과 E2E 회귀 테스트에 한정한다.
- 선택지는 dependency patch가 아니라 package-owned adapter guard이며, 기존 지침의 최소 수정 원칙과 GridStack adapter boundary rule에 부합한다.
- Git commit, push, PR 생성은 Supervisor 명시 승인 전 수행하지 않는다.

## File Structure

- Modify: `/Users/kim1124/Dev/kmsf/packages/gridstack/test/playwright/specs/dashboard-grid.spec.ts`
  - browser-boundary resize 종료 누락을 재현하는 Playwright RED 테스트를 추가한다.
  - 기존 helper 근처에 interaction state 및 browser-boundary simulation helper를 추가한다.
- Modify: `/Users/kim1124/Dev/kmsf/packages/gridstack/src/gridstack/adapter.ts`
  - `createDashboardGridAdapter()` 내부 interaction state에 boundary guard listener와 fallback frame을 추가한다.
  - GridStack cleanup path를 우선 사용한다.
  - Implementation note: same-widget follow-up resize recovery uses GridStack's public `prepareDragDrop(item, true)` path. The adapter does not directly delete `_initDD` or `ddElement`.
- Modify: `/Users/kim1124/Dev/kmsf/packages/gridstack/docs/agents/gridstack/research.md`
  - RED/GREEN 후 검증된 root cause와 최종 guard 동작을 기록한다.
- Modify: `/Users/kim1124/Dev/kmsf/packages/gridstack/docs/agents/gridstack/plan.md`
  - 2026-06-08 계획을 implemented 상태로 갱신한다.
- Modify: `/Users/kim1124/Dev/kmsf/packages/gridstack/docs/agents/gridstack/memory.md`
  - 다음 에이전트가 이어받을 수 있는 compact checkpoint를 추가한다.
- Modify: `/Users/kim1124/Dev/kmsf/packages/gridstack/reports/2026-06-08.md`
  - 실제 실행 명령, pass/fail, residual risk를 append한다.

## Task 1: Add Browser-Boundary RED E2E

**Files:**
- Modify: `/Users/kim1124/Dev/kmsf/packages/gridstack/test/playwright/specs/dashboard-grid.spec.ts`

- [x] **Step 1: Add interaction-state and boundary simulation helpers**

Insert after `readGridEngineColumn()`.

```ts
async function readWidgetInteractionState(widget: Locator) {
  return widget.evaluate((element) => ({
    isResizing: element.classList.contains("ui-resizable-resizing"),
    hasInlinePosition: (element as HTMLElement).style.position === "absolute",
  }));
}

async function simulateBrowserBoundaryExit(page: Page) {
  const viewport = page.viewportSize() ?? { width: 1280, height: 720 };

  await page.mouse.move(viewport.width - 1, viewport.height - 1, { steps: 8 });
  await page.evaluate(() => {
    document.documentElement.dispatchEvent(
      new MouseEvent("mouseleave", {
        bubbles: true,
        cancelable: true,
        buttons: 1,
        clientX: window.innerWidth + 24,
        clientY: window.innerHeight + 24,
        relatedTarget: null,
      }),
    );
    window.dispatchEvent(new Event("blur"));
  });
}
```

- [x] **Step 2: Add the failing browser-boundary regression**

Insert after `defers grid sync while a widget is actively resizing`.

```ts
test("finalizes widget resize when the pointer leaves the browser boundary", async ({ page }, testInfo) => {
  test.skip(testInfo.project.name !== "chromium", "Pointer interaction regression runs on the desktop project only.");

  const diagnostics = collectBrowserDiagnostics(page);

  await page.goto("/");

  const grid = page.getByTestId("dashboard-grid");
  const sales = page.getByTestId("dashboard-widget-sales");

  await expect(grid).toHaveAttribute("data-columns", "6");

  try {
    const { startX, startY } = await startWidgetResize(page, sales);
    await page.mouse.move(startX + 140, startY + 110, { steps: 8 });

    await page.getByLabel("컬럼 선택").evaluate((element) => {
      const select = element as HTMLSelectElement;
      select.value = "12";
      select.dispatchEvent(new Event("change", { bubbles: true }));
    });

    await expect.poll(() => readGridEngineColumn(grid)).toBe(6);

    await simulateBrowserBoundaryExit(page);

    await expect.poll(async () => (await readWidgetInteractionState(sales)).isResizing).toBe(false);
    await expect(grid).toHaveAttribute("data-columns", "12");
    await expect.poll(() => readGridEngineColumn(grid)).toBe(12);
    expect(diagnostics).toEqual([]);
  } finally {
    await page.mouse.up().catch(() => undefined);
  }
});
```

- [x] **Step 3: Run RED**

Run from `/Users/kim1124/Dev/kmsf`.

```bash
npm --workspace=@kmsf/gridstack run test:e2e -- test/playwright/specs/dashboard-grid.spec.ts --project=chromium -g "browser boundary"
```

Expected: FAIL. Expected failure is `isResizing` remains `true`, or grid engine column remains `6` instead of applying pending `12`.

Stop condition: If the test fails before resize starts, fails due to selector issues, or throws console/page errors unrelated to the boundary condition, do not modify production code. Fix the test setup or report the blocker first.

## Task 2: Add Adapter Boundary Exit Guard

**Files:**
- Modify: `/Users/kim1124/Dev/kmsf/packages/gridstack/src/gridstack/adapter.ts`

- [x] **Step 1: Add local pointer snapshot type**

Insert after `DashboardGridAdapter<TData = unknown>`.

```ts
type PointerSnapshot = {
  clientX: number;
  clientY: number;
};
```

- [x] **Step 2: Add guard state**

Inside `createDashboardGridAdapter()`, extend the existing frame/state variables.

```ts
  let finishInteractionFrame: number | undefined;
  let deferredSyncFrame: number | undefined;
  let forceEndFrame: number | undefined;
  let lastPointer: PointerSnapshot | undefined;
  let interactionGuardsAttached = false;
```

- [x] **Step 3: Add guard helper functions**

Insert after `cancelFrame()`.

```ts
  const captureInteractionPointer = (event: MouseEvent) => {
    if (!isInteracting) {
      return;
    }
    lastPointer = { clientX: event.clientX, clientY: event.clientY };
  };

  const forceInteractionEnd = () => {
    if (!isInteracting || forceEndFrame !== undefined) {
      return;
    }

    const point = lastPointer ?? { clientX: 0, clientY: 0 };
    document.dispatchEvent(
      new MouseEvent("mouseup", {
        bubbles: true,
        cancelable: true,
        button: 0,
        buttons: 0,
        clientX: point.clientX,
        clientY: point.clientY,
      }),
    );

    forceEndFrame = window.requestAnimationFrame(() => {
      forceEndFrame = undefined;
      if (isInteracting) {
        stopInteraction();
      }
    });
  };

  const handleDocumentMouseMove = (event: MouseEvent) => {
    captureInteractionPointer(event);
    if (event.buttons === 0) {
      forceInteractionEnd(event);
    }
  };

  const handleDocumentMouseLeave = (event: MouseEvent) => {
    captureInteractionPointer(event);
    if (event.relatedTarget !== null) {
      return;
    }
    if (event.buttons === 0) {
      forceInteractionEnd(event, true);
    }
  };

  const handleVisibilityChange = () => {
    if (document.visibilityState === "hidden") {
      forceInteractionEnd();
    }
  };

  const attachInteractionGuards = () => {
    if (interactionGuardsAttached) {
      return;
    }
    interactionGuardsAttached = true;
    document.addEventListener("mousemove", handleDocumentMouseMove, true);
    document.documentElement.addEventListener("mouseleave", handleDocumentMouseLeave, true);
    document.addEventListener("visibilitychange", handleVisibilityChange);
  };

  const detachInteractionGuards = () => {
    if (!interactionGuardsAttached) {
      return;
    }
    interactionGuardsAttached = false;
    document.removeEventListener("mousemove", handleDocumentMouseMove, true);
    document.documentElement.removeEventListener("mouseleave", handleDocumentMouseLeave, true);
    window.removeEventListener("blur", forceInteractionEnd);
    document.removeEventListener("visibilitychange", handleVisibilityChange);
  };
```

TypeScript note: `forceInteractionEnd()` references `stopInteraction()`, which is declared later in the same closure. This is valid because the function is not invoked until after adapter initialization and event binding complete.

- [x] **Step 4: Update interaction start**

Replace existing `startInteraction()`.

```ts
  const startInteraction = (event?: Event) => {
    isInteracting = true;
    cancelFrame(finishInteractionFrame);
    cancelFrame(forceEndFrame);
    finishInteractionFrame = undefined;
    forceEndFrame = undefined;
    if (event instanceof MouseEvent) {
      captureInteractionPointer(event);
    }
    attachInteractionGuards();
  };
```

- [x] **Step 5: Update interaction flush and stop cleanup**

At the beginning of `flushInteraction()`, add guard cleanup.

```ts
  const flushInteraction = () => {
    finishInteractionFrame = undefined;
    detachInteractionGuards();
    lastPointer = undefined;
    isInteracting = false;

    const shouldCommit = pendingCommit;
    const shouldSync = pendingSync;
    pendingCommit = false;
    pendingSync = false;

    if (shouldCommit) {
      commitLayout();
    }
    if (shouldSync) {
      scheduleDeferredSync();
    }
  };
```

Replace existing `stopInteraction()`.

```ts
  const stopInteraction = () => {
    pendingCommit = true;
    detachInteractionGuards();
    cancelFrame(forceEndFrame);
    cancelFrame(finishInteractionFrame);
    forceEndFrame = undefined;
    finishInteractionFrame = window.requestAnimationFrame(flushInteraction);
  };
```

- [x] **Step 6: Update destroy cleanup**

Inside `destroy()`, before `grid.offAll()`, add:

```ts
      detachInteractionGuards();
      cancelFrame(forceEndFrame);
```

Full destroy block should become:

```ts
    destroy() {
      detachInteractionGuards();
      cancelFrame(finishInteractionFrame);
      cancelFrame(deferredSyncFrame);
      cancelFrame(forceEndFrame);
      grid.offAll();
      grid.destroy(false);
    },
```

- [x] **Step 7: Run GREEN for browser-boundary test**

Run from `/Users/kim1124/Dev/kmsf`.

```bash
npm --workspace=@kmsf/gridstack run test:e2e -- test/playwright/specs/dashboard-grid.spec.ts --project=chromium -g "browser boundary"
```

Expected: PASS, 1 test.

If it fails because `isResizing` clears but engine column remains `6`, inspect whether `pendingSync` is set before force end and whether `flushInteraction()` schedules `scheduleDeferredSync()`.

## Task 3: Add Grid-Area Exit Coverage

**Files:**
- Modify: `/Users/kim1124/Dev/kmsf/packages/gridstack/test/playwright/specs/dashboard-grid.spec.ts`

- [x] **Step 1: Add document-mouseup grid-area test**

Insert near the browser-boundary regression.

```ts
test("finishes widget resize after leaving the grid area", async ({ page }, testInfo) => {
  test.skip(testInfo.project.name !== "chromium", "Pointer interaction regression runs on the desktop project only.");

  const diagnostics = collectBrowserDiagnostics(page);

  await page.goto("/");

  const grid = page.getByTestId("dashboard-grid");
  const sales = page.getByTestId("dashboard-widget-sales");
  const beforeResize = await readWidgetLayout(sales);
  const gridBox = await grid.boundingBox();

  if (!gridBox) {
    throw new Error("Grid bounding box is not available");
  }

  const { startX, startY } = await startWidgetResize(page, sales);

  await page.mouse.move(startX + 180, startY + 120, { steps: 8 });
  await page.mouse.move(gridBox.x + gridBox.width + 80, startY + 120, { steps: 8 });
  await page.mouse.up();

  await expect.poll(async () => (await readWidgetInteractionState(sales)).isResizing).toBe(false);
  await expect.poll(async () => {
    const layout = await readWidgetLayout(sales);
    return layout.w !== beforeResize.w || layout.h !== beforeResize.h;
  }).toBe(true);
  expect(diagnostics).toEqual([]);
});
```

- [x] **Step 2: Extend browser-boundary test with follow-up resize**

After the `readGridEngineColumn(grid)).toBe(12)` assertion in the browser-boundary test, add:

```ts
    const afterForcedEnd = await readWidgetLayout(sales);
    await resizeWidget(page, sales, 80, 60);
    await expect.poll(async () => {
      const layout = await readWidgetLayout(sales);
      return layout.w !== afterForcedEnd.w || layout.h !== afterForcedEnd.h;
    }).toBe(true);
```

This confirms the forced cleanup does not leave GridStack in a broken state for the next resize.

- [x] **Step 3: Run focused pointer regression set**

Run from `/Users/kim1124/Dev/kmsf`.

```bash
npm --workspace=@kmsf/gridstack run test:e2e -- test/playwright/specs/dashboard-grid.spec.ts --project=chromium -g "browser boundary|leaving the grid area|defers grid sync"
```

Expected: PASS, 3 tests.

## Task 4: Run Package Verification

**Files:**
- No source edits expected in this task.

- [x] **Step 1: Run package baseline**

Run from `/Users/kim1124/Dev/kmsf`.

```bash
npm --workspace=@kmsf/gridstack run verify
```

Expected: PASS. This runs lint, Vitest, and build.

- [x] **Step 2: Run package full gate**

Run from `/Users/kim1124/Dev/kmsf`.

```bash
npm --workspace=@kmsf/gridstack run verify:full
```

Expected: PASS. Existing mobile skips for desktop pointer-only tests may remain unchanged.

If Playwright webServer fails with `listen EPERM` or a similar sandbox bind error, retry only if escalation is available and policy allows it. If it remains blocked, record the exact blocker in `reports/2026-06-08.md`.

## Task 5: Update Documentation And Report

**Files:**
- Modify: `/Users/kim1124/Dev/kmsf/packages/gridstack/docs/agents/gridstack/research.md`
- Modify: `/Users/kim1124/Dev/kmsf/packages/gridstack/docs/agents/gridstack/plan.md`
- Modify: `/Users/kim1124/Dev/kmsf/packages/gridstack/docs/agents/gridstack/memory.md`
- Modify: `/Users/kim1124/Dev/kmsf/packages/gridstack/reports/2026-06-08.md`

- [x] **Step 1: Update research**

Append:

```md
## 2026-06-08 Resize Boundary Exit Implementation Findings

- Browser-boundary exit can be covered in Playwright by starting a resize, dispatching a `mouseleave` with `relatedTarget: null`, and dispatching `window.blur` while document `mouseup` is intentionally absent.
- The adapter fix keeps GridStack as the primary cleanup owner by dispatching a synthetic document `mouseup` before falling back to adapter `stopInteraction()`.
- Boundary guard listeners are attached only during active drag/resize and removed on stop, flush, and destroy.
```

- [x] **Step 2: Update plan summary**

In `docs/agents/gridstack/plan.md`, under `2026-06-08 Resize Boundary Exit Plan`, add:

```md
7. Implementation status: completed after RED/GREEN Playwright validation and package verification.
```

Only add this line after tests pass. If verification is blocked, write:

```md
7. Implementation status: blocked. See `reports/2026-06-08.md` for the exact command failure and blocker.
```

- [x] **Step 3: Update memory checkpoint**

Append:

```md
- 2026-06-08: resize 중 browser-boundary exit로 document `mouseup`이 누락되는 경로를 Playwright로 재현하고, adapter에서 active interaction 동안만 `mouseleave`/`blur`/`visibilitychange` guard를 붙여 GridStack cleanup을 synthetic `mouseup`으로 유도했다. stop event가 오지 않으면 다음 animation frame에서 adapter flush로 fallback한다.
```

- [x] **Step 4: Append report entry**

Append an entry to `/Users/kim1124/Dev/kmsf/packages/gridstack/reports/2026-06-08.md` with these exact sections:

- header: current KST time from `date '+%H:%M KST'` and title `GridStack resize boundary exit implementation`.
- timestamp: exact output from `date '+%Y-%m-%d %H:%M:%S %Z'`.
- summary:
  - Added Playwright coverage for resize completion after grid-area exit and browser-boundary exit.
  - Added adapter-level boundary guard cleanup for active drag/resize interactions.
  - Verified pending layout sync flushes after forced resize completion.
- changed files:
  - `packages/gridstack/src/gridstack/adapter.ts`
  - `packages/gridstack/test/playwright/specs/dashboard-grid.spec.ts`
  - `packages/gridstack/docs/agents/gridstack/research.md`
  - `packages/gridstack/docs/agents/gridstack/plan.md`
  - `packages/gridstack/docs/agents/gridstack/memory.md`
  - `packages/gridstack/reports/2026-06-08.md`
- commands actually run: list each RED, GREEN, focused regression, and package verification command with its observed pass/fail result.
- pass/fail result: state the exact Playwright and package verification result counts.
- residual risks or blockers: keep `Browser-window release is simulated with mouseleave/buttons=0 movement; a headed/manual check remains useful for OS-level release behavior.` unless a stronger manual verification is completed.

## Self-Review Checklist

- Requirement coverage: Task 1 E2E reproduction, Task 2 runtime fix, Task 3 GridLayout/follow-up coverage, Task 4 verification, Task 5 docs/report.
- Scope control: no public API, dependency, lockfile, or unauthorized Git operation changes.
- Final update: DD binding recovery uses `grid.prepareDragDrop(item, true)` instead of adapter-side internal flag deletion.
