# 2026-05-02

## 23:32 KST - create-kmsf external npx packaging

### Summary

- `create-kmsf`를 monorepo 내부 wrapper에서 외부 tarball 기반 `npx` 실행 가능한 self-contained package 구조로 보완했다.
- `@kmsf/generator-core` 런타임 의존성을 제거하고 generator core source를 package-local `src/generator-core`로 vendoring했다.
- `templates/next-app-base`를 package-local template으로 포함하고, packed tarball에서 npm이 `.gitignore`를 제외하는 문제를 `gitignore` placeholder 복원 방식으로 처리했다.
- `--silent`는 prompt를 열지 않고 필수 옵션 누락 시 즉시 실패하도록 변경했다.

### Changed Files

- `package.json`
- `README.md`
- `src/args.ts`
- `src/cli.ts`
- `src/logger.ts`
- `src/prompts.ts`
- `src/generator-core/**/*`
- `templates/next-app-base/**/*`
- `test/*.test.ts`
- `test/packaging.test.ts`
- `docs/superpowers/specs/2026-05-02-external-npx-design.md`
- `docs/superpowers/plans/2026-05-02-external-npx-packaging.md`
- `../../package-lock.json`

### Commands Actually Run

- `npm run test:run -- test/packaging.test.ts test/prompts.test.ts` - RED confirmed 4 expected failures.
- `npm run test:run -- test/packaging.test.ts` - RED confirmed missing `gitignore` placeholder.
- `npm install --package-lock-only --ignore-scripts` from repository root.
- `npm run lint`
- `npm run typecheck`
- `npm run build`
- `npm run test:run`
- `npm_config_cache=/private/tmp/kmsf-npm-cache npm pack --dry-run`
- `npm_config_cache=/private/tmp/kmsf-npm-cache npm pack --pack-destination /private/tmp`
- `npm_config_cache=/private/tmp/kmsf-npm-cache npx --yes --package /private/tmp/create-kmsf-0.1.0.tgz -- create-kmsf smoke-app --auth=local-json --no-i18n --no-install --no-git --no-playwright --silent`
- `test -f smoke-app/.gitignore && test ! -f smoke-app/gitignore && test -f smoke-app/package.json`

### Result

- PASS: `npm run lint`
- PASS: `npm run typecheck`
- PASS: `npm run build`
- PASS: `npm run test:run` - 7 files, 30 tests passed.
- PASS: `npm pack --dry-run` - tarball includes `dist/generator-core/**/*`, `templates/next-app-base/**/*`, and `templates/next-app-base/gitignore`.
- PASS: local tarball `npx --package` smoke created a project and restored `.gitignore`.

### Residual Risks / Blockers

- 실제 GitHub release URL 기반 remote tarball 실행은 아직 URL이 없어서 검증하지 않았다.
- 완전히 비어 있는 npm cache에서는 `picocolors`, `prompts` 다운로드를 위해 registry network가 필요하다.
- `npm install --package-lock-only --ignore-scripts` 결과 npm audit 기준 moderate vulnerability 2건이 보고되었으며, 이번 범위에서는 dependency upgrade를 수행하지 않았다.
