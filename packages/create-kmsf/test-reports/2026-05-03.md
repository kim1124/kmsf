# create-kmsf Work Report

## 2026-05-03 00:18:31 KST

### Summary

`charts` 기준 agent harness 모델에 맞춰 CLI, generator-core, template, test 경계별 지침과 `docs/agents` map을 추가했다.

### Changed Files

- `AGENTS.md`
- `GUIDE.md`
- `docs/agents/README.md`
- `docs/agents/{root,src,generator-core,templates,test}/{research,plan,memory}.md`
- `src/AGENTS.md`
- `src/generator-core/AGENTS.md`
- `templates/next-app-base/AGENTS.md`
- `test/AGENTS.md`

### Commands Actually Run

- `find <home>/개발/kmsf/packages -maxdepth 5 ( -name AGENTS.md -o -name GUIDE.md -o -name research.md -o -name plan.md -o -name memory.md ) -print | sort`
- `wc -l <home>/개발/kmsf/packages/{gridstack,create-kmsf,generator-core,data-table}/AGENTS.md <home>/개발/kmsf/packages/{gridstack,create-kmsf,generator-core,data-table}/GUIDE.md`
- `find <home>/개발/kmsf/packages/{gridstack,create-kmsf,generator-core,data-table} -path '*/docs/agents/*/plan.md' -print0 | xargs -0 wc -l`
- `find <home>/개발/kmsf/packages/{gridstack,create-kmsf,generator-core,data-table} -name AGENTS.md -print0 | xargs -0 wc -l`
- `node <<'NODE' ... instruction structure validator ... NODE`

### Result

- PASS: required AGENTS/GUIDE/docs/agents files exist for the affected package.
- PASS: all AGENTS.md files are under 100 lines.
- PASS: all docs/agents plan.md files are under 500 lines.
- NOT RUN: package code lint/test/build was not run because this was instruction-only documentation work and no runtime source was changed in this package by this task.

### Residual Risks / Blockers

- 기존 CLI/runtime/test source에는 이번 작업으로 추가 변경을 하지 않았다.
- `create-kmsf`에는 이 작업 전부터 별도 modified 파일들이 있어, 코드 테스트는 instruction-only 검증과 분리했다.
