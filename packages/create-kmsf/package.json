{
  "name": "create-kmsf",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "create-kmsf": "./bin/create-kmsf.js"
  },
  "files": [
    "bin",
    "dist",
    "templates",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit",
    "prepack": "npm run build",
    "prepublishOnly": "npm run build",
    "verify": "npm run lint && npm run test:run && npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "picocolors": "^1.1.1",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "@types/prompts": "^2.4.9",
    "typescript": "^6.0.3",
    "vitest": "^4.1.4"
  }
}
