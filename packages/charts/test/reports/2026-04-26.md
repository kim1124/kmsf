# 2026-04-26

## 15:47 KST - 외부 개발자 사용성 보강

- 작업 요약:
  - `GaugeChart`, `SunburstChart` alias를 추가하고 기존 `GuageChart`, `SunbustChart` 호환 export를 유지했다.
  - `createTrendRows`, `createTopRows` helper를 추가해 tuple 포맷을 직접 작성하지 않아도 되도록 했다.
  - `WordCloud`의 `echarts-wordcloud` top-level import를 browser lazy load로 변경해 public API import가 SSR/test 환경에서 깨지지 않도록 했다.
  - Vite 예제에서 6개 차트 전체를 렌더링하도록 확장했다.
  - 빠른 시작 문서와 consumer usability 설계/계획 문서를 추가했다.
- 변경 파일:
  - `README.md`
  - `docs/03-component-api-draft.md`
  - `docs/06-quick-start.md`
  - `docs/README.md`
  - `docs/superpowers/specs/2026-04-26-kmsf-charts-consumer-usability-design.md`
  - `docs/superpowers/plans/2026-04-26-kmsf-charts-consumer-usability.md`
  - `example/src/App.tsx`
  - `example/src/styles.css`
  - `src/common/data-builders.ts`
  - `src/components/WordCloud/WordCloud.tsx`
  - `src/index.ts`
  - `test/playwright/specs/example.spec.ts`
  - `test/vitest/public-api.test.ts`
  - `test/reports/2026-04-26.md`
- 수행한 검증:
  - `npm --workspace=@kmsf/charts run test:run -- test/vitest/public-api.test.ts` - sandbox EPERM 후 권한 상승 재실행, initial RED 확인
  - `npm --workspace=@kmsf/charts run test:run -- test/vitest/public-api.test.ts` - pass, 3 tests
  - `npm --workspace=@kmsf/charts run lint` - pass
  - `npm --workspace=@kmsf/charts run test:run` - pass, 3 files / 11 tests
  - `npm --workspace=@kmsf/charts run build` - pass
  - `npm --workspace=@kmsf/charts run test:e2e` - pass, chromium/mobile-chrome 2 passed
  - `npm --workspace=@kmsf/charts run verify` - pass
  - `npm audit --audit-level=moderate --json` - 권한 상승 재실행으로 확인, moderate 3건
- 결과:
  - pass
- 잔여 이슈 및 후속 작업:
  - `npm audit` 기준 `next`, `next-intl`, `postcss` 경로의 moderate 취약점 3건이 남아 있다. 제안 fix는 semver major downgrade 형태라 이번 작업 범위에서 적용하지 않았다.
  - `GaugeChart`, `SunburstChart`는 alias로 추가했으며 기존 요청 표기 export는 호환성을 위해 유지한다.
  - `dist` build 산출물은 소스 산출물이 아니므로 검증 후 정리했다.

## 16:56 KST - 예제 차트 동작 및 컨테이너 resize 수정

- 작업 요약:
  - TrendChart 기본 dataZoom에서 하단 slider를 제거하고 inside zoom만 유지했다.
  - TopChart Column 라벨 회전은 label/container overflow가 예상될 때만 적용되도록 helper를 추가했다.
  - Sankey, WordCloud, Gauge, Sunburst 기본 legend를 숨겼다.
  - WordCloud 텍스트 컬러를 랜덤 팔레트로 표시하도록 했다.
  - Gauge 데이터 정규화와 formatter를 추가해 NaN 표시를 방지했다.
  - Sunburst hover 시 ancestor focus와 labelLine 표시 옵션을 추가했다.
  - 예제 페이지를 상단 제목, 좌측 차트 메뉴/샘플 데이터, 중앙 500px 차트 stage 레이아웃으로 재구성했다.
  - 차트 viewport가 제목을 제외한 남은 컨텐츠 영역에 맞춰 resize되도록 flex 구조를 적용했다.
- 변경 파일:
  - `src/common/options.ts`
  - `src/components/TrendChart/TrendChart.tsx`
  - `src/components/TopChart/TopChart.tsx`
  - `src/components/SankeyChart/SankeyChart.tsx`
  - `src/components/WordCloud/WordCloud.tsx`
  - `src/components/GuageChart/GuageChart.tsx`
  - `src/components/SunbustChart/SunbustChart.tsx`
  - `example/src/App.tsx`
  - `example/src/styles.css`
  - `test/vitest/options.test.ts`
  - `test/playwright/specs/example.spec.ts`
  - `test/reports/2026-04-26.md`
- 수행한 검증:
  - `npm --workspace=@kmsf/charts run test:run -- test/vitest/options.test.ts` - initial RED 확인, 신규 helper 미구현으로 3 tests failed
  - `npm --workspace=@kmsf/charts run test:e2e -- --project=chromium -g "chart canvas resizes"` - initial RED 확인, canvas bottom이 stage bottom을 초과
  - `npm --workspace=@kmsf/charts run test:run -- test/vitest/options.test.ts` - pass, 7 tests
  - `npm --workspace=@kmsf/charts run lint` - pass
  - `npm --workspace=@kmsf/charts run test:run` - pass, 3 files / 14 tests
  - `npm --workspace=@kmsf/charts run build` - pass
  - `npm --workspace=@kmsf/charts run test:e2e` - pass, chromium/mobile-chrome 6 passed
  - `npm --workspace=@kmsf/charts run verify` - pass
- 결과:
  - pass
- 잔여 이슈 및 후속 작업:
  - 기존 `npm audit` moderate 3건은 이번 변경 범위와 무관하게 남아 있다.
  - `dist` build 산출물은 검증 후 정리했다.

## 17:32 KST - 혼합형 예제 보강 및 Trend 실시간 애니메이션 수정

- 작업 요약:
  - Superpowers 절차에 맞춰 혼합형 예제 설계/계획 문서를 추가했다.
  - TrendChart 실시간 전체 교체 갱신 시 ECharts point update interpolation을 끄도록 series/top-level animation 기본값을 조정했다.
  - TrendChart 샘플 데이터는 60개 window를 유지하며 오래된 데이터가 앞에서 빠지고 최신 데이터가 뒤로 들어오도록 시간 값을 단조 증가 형태로 수정했다.
  - 좌측 샘플 패널에 선택 차트의 데이터와 사용 코드 예시를 함께 표시하도록 보강했다.
  - Playwright canvas 검증을 전체 격자 샘플링 방식으로 강화해 각 차트가 실제로 non-blank 렌더링되는지 확인했다.
- 변경 파일:
  - `docs/superpowers/specs/2026-04-26-kmsf-charts-example-usability-design.md`
  - `docs/superpowers/plans/2026-04-26-kmsf-charts-example-usability.md`
  - `src/common/data-normalizers.ts`
  - `src/components/TrendChart/TrendChart.tsx`
  - `example/src/App.tsx`
  - `example/src/styles.css`
  - `test/vitest/data-normalizers.test.ts`
  - `test/playwright/specs/example.spec.ts`
  - `test/reports/2026-04-26.md`
- 수행한 검증:
  - `npm --workspace=@kmsf/charts run test:run -- test/vitest/data-normalizers.test.ts` - initial RED 확인, Trend realtime animation option 없음으로 1 test failed
  - `npm --workspace=@kmsf/charts run test:run -- test/vitest/data-normalizers.test.ts` - pass, 5 tests
  - `npm --workspace=@kmsf/charts run lint` - pass
  - `npm --workspace=@kmsf/charts run test:run` - pass, 3 files / 15 tests
  - `npm --workspace=@kmsf/charts run test:e2e` - first run failed due canvas sampling too narrow, test corrected to full-grid sampling
  - `npm --workspace=@kmsf/charts run test:e2e` - pass, chromium/mobile-chrome 6 passed
  - `npm --workspace=@kmsf/charts run verify` - pass
- 결과:
  - pass
- 잔여 이슈 및 후속 작업:
  - 기존 `npm audit` moderate 3건은 이번 변경 범위와 무관하게 남아 있다.
  - `dist` build 산출물은 소스 산출물이 아니므로 검증 후 정리했다.
