# SunbustChart

계층형 Pie 차트인 Sunburst 계열 컴포넌트 위치다.

요청 표기인 `SunbustChart`를 초기 public API 이름으로 유지한다.

후속 구현 기준:

- 계층 데이터는 ECharts 공식 `children` 구조 유지
- 상세 ECharts option은 `seriesOptions`와 `options`로 override
