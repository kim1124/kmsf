# Components Memory

## 2026-06-09

- Decision: current public exports use `GaugeChart` and `SunburstChart`; misspelled `GuageChart` and `SunbustChart` are not compatibility exports.
- Decision: `GenericChart`, `RadarChart`, `HeatmapChart`, and `GraphChart` are part of the public component surface.

## 2026-05-02

- Superseded decision: keep requested misspelled exports until a compatibility plan is approved.
- Decision: public API changes need alias and documentation review.
- Decision: chart rendering regressions are browser-verification blockers.
