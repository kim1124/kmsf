# Next Monorepo Template

`templates/next-monorepo`는 향후 monorepo scaffold 후보 디렉터리다.

현재 KMSF 저장소 자체는 npm workspaces monorepo지만, `create-kmsf` production catalog는 standalone `next-app-base` 생성을 우선한다.

## 현재 상태

- 구조 고정용 placeholder
- 생성 경로 아님
- package/app 분리 정책이 확정된 뒤 scaffold source로 승격 가능
